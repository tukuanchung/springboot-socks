spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot?useUnicode=true&characterEncoding=utf-8
    username: root
    password: NewPassword
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
logging:
  level:
    root: info
    com.example: debug
  file:
    name: logs/debug_advanced.log

